import modulo2 #importamos el módulo 2 de la siguiente manera

print(modulo2.diccionario.items()) # podemos acceder a los elementos de su diccionario
print(modulo2.suma(2,3,7)) # de esta manera podemos acceder a su función 'suma', la cual recibe 3 parámetros 
